# Copy to .env.local and fill in real values for local development
# For local dev: leave empty — Vite proxy handles /api -> localhost:8000
VITE_API_URL=

# For production build pointing to Render:
# VITE_API_URL=https://ethara-backend.onrender.com
